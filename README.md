# CVPro — Générateur de CV professionnel avec IA

## 🚀 Déploiement GitHub → Vercel

```bash
git init && git add . && git commit -m "CVPro"
git remote add origin https://github.com/TON_PSEUDO/cvpro.git
git push -u origin main
```
Vercel → Add New Project → importer → Framework: **Other** → Deploy

## 🔐 Admin
URL : `/admin-panel` | Login : `admin` | MDP : `admin2024`
**Changer le mot de passe immédiatement dans Sécurité**

## ⚙️ Configuration
1. Admin → **IA** → coller la clé API → Enregistrer
2. Admin → **Paiements** → Clé CinetPay + Site ID → Enregistrer
